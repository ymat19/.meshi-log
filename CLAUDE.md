# .meshi-log

個人用の食事記録ダッシュボード。チャットに食事の写真を送ると、エージェントが
栄養成分を推定 → 対話で訂正 → JSON としてリポジトリにコミットし、Cloudflare
Workers 上のダッシュボードに反映される。**総合的で一般的な食生活改善**を主目的と
し、栄養成分の記録・集計・統計を行う。特定の単一指標（塩分・糖質など）の管理を
目的化しない。

### 提案・アドバイスの姿勢（重要）

**続けられる食生活の改善が目的。** 記録に対して助言・提案をするときは以下を守る。

- **各栄養素の目標値・基準は一切変えない。** 塩分を含むすべての目標・基準は従来
  どおり維持する。見直すのは**優先度（重み付け）だけ**——特定の1指標を突出して
  重視するのをやめ、全指標を**対等にバランスで**見る。
- **総合的・現実的に見る。** たんぱく質・エネルギー・脂質・炭水化物・食物繊維・
  塩分などを、どれか1つに偏らず全体のバランスと1日〜数日のトレンドで判断する。
  **単一指標の最小化を目的化して選択肢を格下げしない**（例：1g の塩分差で、本人が
  食べたいものを却下する、はしない）。目標を緩めるのではなく、優先順位を正す。
- **ユーザーが食べたいもの・手元の食材を尊重する。** 満足感や持続可能性を軽視して、
  味気ない・非現実的な代替（素うどんだけ等）を「健康的だから」と押し付けない。
  **続かない提案は無価値**。ユーザーが食べたいものが決まっているなら、それを前提に
  現実的な整え方を助言する。
- **説教しない。** 単一指標を根拠にした否定・ダメ出し・くどい注意を繰り返さない。
  医師の指示（例「毎食炭水化物を摂る」）があればそれを最優先で尊重する。
- 助言は簡潔に。求められた問い（量・選択・可否）にまっすぐ答え、余計なランキングや
  こじつけの理由を並べない。

## 技術スタック

- Vite + React + TypeScript
- **デプロイは Cloudflare Workers**（静的アセット配信、`wrangler.jsonc`）。
  リポジトリを Cloudflare に連携しており、`main` への push のたびに Workers Builds
  がビルドして公開する。**ビルド成果物（dist/）はコミットしない**（`.gitignore`
  済み）。生成物を Git 管理しないため source/dist の drift が起きない、という
  のが標準プラクティス。
- データ（月別 JSON・画像）は `public/` に置き、ビルド時に dist へコピーされ、
  Workers の静的アセットとして配信される。記録を追加 → push すると Cloudflare が
  再デプロイ（数十秒〜1分のラグ）。
- CI（`.github/workflows/ci.yml`）は PR でビルドを検証するのみ（デプロイはしない）。
  `@cloudflare/vite-plugin` は Node 22+ を要求するため、CI も Node 22 を使う。

## ディレクトリ構成

```
index.html               Vite エントリ
wrangler.jsonc           Cloudflare Workers の設定（静的アセット配信）
.github/workflows/ci.yml       PR でのビルド検証（デプロイはしない）
src/                      アプリ本体（mock/real を一切意識しない）
  data/
    types.ts              全データの型（唯一の真実）
    config.ts             栄養素・食事区分の定義（両ソースが返す）
    MealDataSource.ts     データソースのインターフェース
    HttpDataSource.ts     実データ実装（public/data を fetch）
    createDataSource.ts   ★唯一の分岐点（?mock を見て実装を選ぶ）
    DataSourceContext.tsx Provider と useMealData フック
    mock/
      MockDataSource.ts   モック実装
      generator.ts        シード付き決定論的データ生成器
      sampleImages.ts     SVG data URI のダミー写真
  lib/nutrition.ts        集計ユーティリティ
  components/             DailySummary / Timeline
public/                   ビルドに含める静的ファイル（ランタイムデータ）
  data/index.json         存在する月のリスト {"months": ["YYYY-MM"]}
  data/YYYY-MM.json       その月の MealEntry 配列
  images/YYYY/MM/...       食事写真
dist/                     ビルド出力（git 管理外）
```

## モックモード

- `?mock=1` を付けるとモック、無ければ実データ。**切り替えは `createDataSource()`
  の 1 箇所だけ**。アプリ本体・コンポーネントには mock/real の分岐を一切書かない。
- モックはシード付き生成器で約 90 日分を決定論的に生成。データが少なくても
  ダッシュボードをフルにプレビューできる。
- 原則：**モックと実データはデータソース以外まったく同一**。新機能を追加するときも
  この不変条件を壊さないこと（アプリ層に mock 判定を持ち込まない）。

## データモデル（MealEntry）

`src/data/types.ts` を参照。要点：
- **栄養成分は `items[].nutrition` が唯一の真実。** 各 item が必須成分を持ち、
  1食の合計はエントリに保存せず `entryTotals()`（`lib/nutrition.ts`）で **item の
  合計として導出**する。合計を別フィールドで二重に持たない（drift しない）。
- `Nutrition` は開いたレコード（型上は全成分 optional）だが、`config.ts` の
  `required` 成分は全 item で必須（`validate-data.ts`／husky/CI が強制）。
  `alcohol_g` のみ任意（飲んだ item にだけ付ける）。
- `schemaVersion` を持たせ、将来のフォーマット変更に備える
- `datetime` は JST オフセット付き ISO8601（例 `2026-06-19T12:30:00+09:00`）

## 開発コマンド

```
npm install
npm run dev          # 開発サーバ。?mock=1 でモックプレビュー
npm run build        # dist/ にビルド（型チェック込み）
npm run preview      # ビルドして wrangler dev でローカル確認（Workers 相当）
npm run deploy       # ビルドして wrangler deploy で本番公開（通常は CI に任せる）
```

## Cloudflare Workers の設定

リポジトリを Cloudflare の Workers Builds に連携している。`main` への push で
自動的にビルド＆公開される（GitHub Actions ではデプロイしない）。Worker 設定は
`wrangler.jsonc`（`assets.not_found_handling: single-page-application` で SPA 配信）。
`@cloudflare/vite-plugin` は Node 22+ を要求するので、ローカル・CI とも Node 22 以上を使う。

公開URL: https://meshi-log.ymat19.workers.dev

## 運用ルール

### 記録時の絶対ルール（過去の失敗から）

- **「今日／昨日／今朝」は必ず日本時間（JST）で判断する。** ハーネスが注入する
  `currentDate` は **UTC 基準**で、日本時間とは日付が1日ズレることがある（UTC 夜＝
  JST 翌朝）。日付に関わる記録をする前に、必ず実時刻を `TZ=Asia/Tokyo date` で確認し、
  JST の「今日」を確定してから「昨日」を逆算する。`currentDate` を鵜呑みにしない。
- **【2026-08-12→13 の失敗から】時刻は「ユーザー発言のたびに」取り直す。前に取った
  値を使い回さない。** `.claude/hooks/jst-now.sh`（`UserPromptSubmit` フック）が毎回
  JST の今日／昨日／一昨日を `<jst-clock>` として注入するので、**日付判断はその最新の
  値だけを見る**。会話の前の方で自分が実行した `date` の出力は、日をまたいで無効に
  なっている可能性があるので絶対に参照しない。実際に、セッション序盤（23:12 JST）に
  取った日付を7時間後（翌 06:22 JST）まで使い回し、ユーザーの「昨日夜」を1日ズラして
  解釈し、既存記録との矛盾に見えたため**本人に二度同じことを言わせる**という事故を
  起こした。
- **日付の矛盾を見つけたら、質問する前にまず自分の時刻認識を疑う。** 「ユーザーの言う
  “昨日” が既存記録と食い違う」ときの第一容疑者は**自分が持っている日付が古いこと**。
  `<jst-clock>` を見直して再計算し、それでも矛盾するときに初めて聞く。時刻を取り直せば
  解けたはずの質問をユーザーに投げるのは、上の「質問の前に必ず自分で調査する」違反。
- **【2026-08-19 の失敗から】主食（ライス・ご飯・麺・パン）を憶測で引き算しない。**
  牛めし・カレー・定食など主食込みの商品は、本人が明示的に「抜いた」と言わない限り
  **既定量（並盛）で必ず計上する**。写真に米粒が写っていないこと、野菜が一緒に写って
  いることは「主食なし」の根拠にならない。本人は糖質制限をしておらず、医師の指示は
  「毎食炭水化物を摂る」。健康的に見える方向へ記録を寄せるのは過少申告であって安全側
  ではない。不明なら `AskUserQuestion` で量を聞く。
- **【2026-08-20 の指摘から】データの矛盾を誤魔化さない。** 同じ食品なのに換算が違う、
  合計が合わない、過去記録と食い違う——を見つけたら、その場で全件引き直して直す。
  「影響が小さい」「揃えるなら直します」と保留して次に進むのは禁止。**矛盾が1つ出た
  時点で、その品目の過去記録は全部意味を失う**（片方だけ直すのも禁止）。
  **公式サイトが 403 で開けないことは、値を確定しない理由にならない**——JAN で商品を
  特定する、ラベル転記の独立ソースを2つ以上突き合わせる等で必ず裏を取る。「開けな
  かったので過去ログを流用」と書いた瞬間、その数値の出所は永久に不明になる。出典は
  食品ごとに1つ（原則 成分表八訂＝fooddb／既製品はラベル表示）に固定し、七訂と八訂を
  混ぜない。`npm run validate` が同一食品の 100g/100ml 換算の食い違いも検出する。
- **【2026-08-20 の指摘から】`item` の名前に注釈を書かない。** 品名に書いてよいのは
  **「食品名（メーカー・商品名を含む）＋分量・実際に食べた内容」だけ**。それ以外は
  すべて entry の `memo` に書く。禁止例——出典・推定方法・確度（「公式表示値」「本人申告」
  「写真からの推定」「部位不明」）、どの料理に入れたか（「まぜそばに投入」「豚汁に追加」
  「ご飯にのせ」）、いつの残りか（「昨日の残り」「夜のぶん」）、シェアの内訳計算
  （「2人で13杯の半分」「2枚入りのうち1枚」）、入手経路（「テイクアウト」「自炊」
  「新潟魚沼工場製」）。
  - **一方、分量と「実際に食べた内容」は品名に必ず書く**（「並盛」「150g」「の半分」
    「みそ汁なし」「ライス大盛」）。公式値から何かを外したなら、**外したという事実は
    内容なので品名に残す**（例:「松屋 うまトマチーズ牛めし（並盛・みそ汁なし）」）。
    ダッシュボードに出るのは品名と数値だけなので、ここを省くと公式と違う数字だけが見える。
  - 注釈が有害なのは見た目の問題ではない。**同じ食品を複数の品名に割り、`validate` の
    同名チェックを無力化する**。実際に卵1個が「生卵 1個」「生卵 1個（まぜそばに投入）」
    「温泉卵/生卵（約50g）」等に分裂し、値が3系統に drift したまま検出されずに残った。
  - `npm run validate` が品名の注釈を検出してコミットを止める（husky/CI）。検査を通す
    ために語を言い換えてごまかすのは禁止。**注釈は消して memo に移す。**
- **ユーザーへの確認・選択肢は必ず `AskUserQuestion`（サジェスト UI）で出す。**
  本文テキストに質問を箇条書きで書いて答えを待つのは禁止。具材・時刻・区分・重複の
  扱いなど、訂正を求めるものはすべてサジェストで提示する。
- **質問の前に必ず自分で調査する。** 既存データ・JST 実時刻・ファイル等で確認できる
  ことは確認してから聞く。確認すれば自明なことを質問に化けさせない（例：日付ズレを
  検証せずに「重複かも」と聞く、は禁止）。
- **写真の品目を名指しする前に、推測語を含まない品目一覧で「本人の常食材」を見る。**
  `grep -o '"name": "[^"]*"' public/data/*.json | sed 's/.*"name": //' | sort | uniq -c | sort -rn`
  自分の推測から作った検索語（「肉じゃが」「豚バラ」等）で引くのは仮説の追認にしかならず、
  仮説が外れていると何も見つからない。**物体を1つずつ名付けて足し算せず、先に「何の料理か」
  を決めてから構成要素を降ろす。** 同定が1点でも覆ったら、その写真から読んだ推定は全部
  捨てて引き直す（部分訂正は禁止）。詳細な手順は log-meal スキルの「写真の同定手順」。
- **「わからないから 0」は絶対に禁止。** 栄養成分の値を埋めるとき、不明・未推定を
  ごまかすための `0` を書いてはならない。`0` を書いてよいのは「その食品にその成分が
  実際に含まれない／ごく微量で 0 とみなせる」ときだけ（例：ストロングゼロの糖質、
  ごはんの食塩）。値が不明なら 0 で穴埋めせず、必ず推定する（既製品は公式の栄養成分
  表示を Web 検索し出典付きで、自炊は標準的な食品成分から推定する）。
  - **必須栄養素（`config.ts` の `required`）は全 item・全エントリ合計で必須。**
    一つでも欠けると `scripts/validate-data.ts` が落ち、husky の pre-commit がコミットを
    止める（CI も落ちる）。記録時は全 item に 8 成分を持たせること。
  - **一食の合計はエントリに持たせない。** 合計は `items[].nutrition` から
    `entryTotals()` で導出される唯一の値。記録時に entry へ合計フィールドを書かない
    （書いても無視され二重管理になる）。これにより合計と内訳の drift が原理的に起きない。
  - 糖質 `sugar_g` は「炭水化物 − 食物繊維」で算出する（データ全体で一貫）。
  - `alcohol_g` だけは任意。飲酒した食品にのみ付け、非飲酒の食品・日には書かない
    （0 も書かない＝欠落させる）。
- **【2026-08-23 の失敗から】調味料は「写っていない」を根拠に落とさない。**
  たれ・ポン酢・醤油・ドレッシングは料理に絡んで沈むので写真にはまず写らない。
  写真に無いことは「かけていない」の証拠にならない（8/19 の「米粒が写っていない
  からライスを引く」と同じ間違い）。実際に、同じカツオのたたきを3食記録する途中で、
  付属のたれを3食目だけ落として塩分を 0.88g → 0.10g に消した。
  - **ユーザーの言葉から在庫の枯渇を推論しない。** 「付属のたれの**残り**をかけた」は
    「残っていたぶんをかけた」であって「使い切った」ではない。言っていないことを
    足して次の記録の前提にしない。
  - **「◯◯無しで計上します、違ったら言ってください」と本文に書くのは禁止。**
    それは確認をユーザーに投げているだけで、確認になっていない。判断できないなら
    `AskUserQuestion` で聞く（1行で済む）。
  - **推定を過小側に倒さない。** 量も調味料も、迷ったら「本人が普通に食べる量」に
    寄せる。安全側のつもりで小さく書くのは過少申告。
  - `npm run validate` が、いつも同じ調味料と一緒に記録されている料理から調味料が
    丸ごと落ちた回を検出してコミットを止める（husky/CI）。

- **【2026-08-24 の失敗から】量は「写真の見た目」からではなく「この人の過去記録」から決める。**
  カツオの茶漬け1食の量を写真の幾何（画素→縮尺→面積→厚み→体積）から出そうとして
  110g→180〜240g→140g→110g→140g→120g と6回変え、実際は **60g** だった。全部が2〜4倍の過大。
  - **なぜ2倍になったか（構造）。** あの計算は縮尺の誤差が**2乗**で効き、厚みは真上からの
    写真に**写らない**。ボケた6.5mmの米粒を1.4倍小さく読んだだけで、面積が2.26倍になり、
    重量がそのまま2倍になった。**この鎖を使う限り2倍は普通に出る。使わないこと。**
  - **もっと悪いのは、はるかに正確な手がかりを持っていながら捨てたこと。** この人の一人前は
    食品ごとにほぼ一定で（パックごはん180g×23件、ザバス200ml×26件、厚揚げ150g×6件）、
    **過去記録を引くだけで写真から読むより桁違いに正確な量が手に入る。**
  - **手順（この順で決める）**
    1. `npm run portions <品名>` で、その食品の基準量（★＝一次情報で裏が取れた量、無ければ
       過去記録の中央値）を引く。**記録を書く前に必ず引く。**
    2. 既製品なら商品の栄養成分表示・内容量が最優先。
    3. 本人が量を言っていればその言葉どおり（1と食い違ってもよい。memo に「本人申告」と書く）。
    4. どれも無ければ**基準量をそのまま使う**。写真は「いつもより多い / いつも通り / 少ない」の
       判定にだけ使い、寸法・面積・体積を計算しない。
  - **ユーザーに「計りに乗せて」と頼まない。** 写真を送るだけで済むのがこのツールの前提で、
    計量を要求するのは機能の否定。量が決まらないなら、聞くのは**分量ではなく品目や食べ方**
    （「いつもと同じくらい？」は聞いてよい）。
  - **測っていない値を「実測」と書かない。** ピクセルを数えていないのに「米粒を物差しに測った」
    「51×45mm」と書いた。**単位と桁の付いた数字は、根拠の無い数字より始末が悪い**——検算
    されるまで本物に見える。
  - **根拠が変わっていないのに数字だけ動かさない。** 6回とも最初に自分が言った110gの周辺に
    着地した。厚みや係数は自分で選べるので、**結論に合う入力を探していただけ**だった。
    量を動かしてよいのは新しい一次情報（本人の言葉・表示値・計量値）が来たときだけ。
  - 同じ失敗を 2026-07-30 のぶりしゃぶ鍋でもやっている（豆腐150gから mm/px を逆算して
    えのき・水菜の量を決めた）。単発ではなく反復した癖として扱う。
  - `npm run validate` が2つ検出してコミットを止める（husky/CI）。(a) memo 内の画素・投影面積・
    mm/px・形状係数・「厚みを仮定」等＝写真からの計測、(b) `scripts/known-portions.json` の
    基準量から1.5倍以上外れた分量で、memo にその品目の根拠（本人申告・表示値）が無いもの。
    **検査を通すために語を言い換えるのは禁止。** 裏が取れた基準量は known-portions.json に足す。

- **【2026-08-25 の失敗から】写真から同定したものは「その人がそれを食べる形か」で必ず潰す。**
  カツオの切り身の写真を「眼とヒレが見える」と読んで、**カツオの頭（あら）**として記録した。
  カツオの頭を食べる人はいないし、この人の記録に一度も出てこない。**過去ログを一度見れば
  消えた読み**だった。前後では、切り身を丸ごと食べる前提の書き方もしている。
  - **ぼけた画素は何にでも見える。画素の側から詰めても止まらない。** 止まるのは
    「食べ方として成立するか」の側だけ。同定を出したら、数値を出す前に必ず
    **(1) その部位を人は食べるか、(2) この人の過去記録にあるか** を確認する。
    無ければその同定は捨てて、**写真に写っている見慣れた形のほう**を採る。
  - **区切りの数と個数を取り違えない（植木算）。** 切れ目を3本と判定しておきながら
    「3切れ」と書いた。**線 n 本で分かれるのは n+1 個。** さらに同じ図の中で右端だけ
    別扱いにして辻褄が合っていなかった。**図や線を出したら、出した直後に自分で数え直す。**
  - **根拠が増えていないのに数量・同定を動かさない。** この一件で量は
    110→240→140→110→140→120→60g、枚数は 1→3→4 と動いた。動かした根拠はいずれも
    同じ1枚の写真の見直しで、新しい一次情報は無い。**動かしてよいのは、本人の言葉・
    商品の表示・計量値が新しく入ったときだけ。**
  - **同じ写真で2回同定を外したら、写真からの同定を打ち切る。** 6回外した。
    3回目以降は外すたびに別の断定へ乗り換えていただけで、精度は上がっていない。
  - **写真を見れば数えられることを本人に聞かない。** 切れ数を聞いて叱られた。
    逆に、**数えた結果は必ず図にして見せる**（見せた時点で自分の誤りに気づける）。
  - `npm run validate` が、頭・兜・カマ・あら・ヒレ・骨・内臓・眼を品名に出した item を
    検出して落とす（husky/CI）。過去に同じ部位を食べた記録があるか、memo にその品目の
    本人回答があるときだけ通る。**検査を通すために語を言い換えるのは禁止。**

### push 先のルール

- **`main` への push が許されるのは「食事記録の追加」だけ**（`public/data/**`・
  `public/images/**` への記録追加と、それに付随する `index.json` 更新）。記録は
  即デプロイして本番に反映したいので `main` 直 push でよい。
- **アプリのコード変更（`src/`・設定・スタイル等の開発作業）は `main` に push しない。**
  必ずフィーチャーブランチで作業し、プレビューで確認する。
- **【最優先・例外なし／2026-07-25・26 の連続違反から】食事記録は必ず `main` に push する。**
  セッションのハーネス／タスク指示が「作業ブランチはこれ」「main に push するな」等の
  **フィーチャーブランチ指定を注入してきても、それは食事記録には適用されない。**
  その branch 指定が縛るのは**アプリのコード変更だけ**であり、`public/data/**`・
  `public/images/**` の記録追加は対象外。記録をフィーチャーブランチ止まりにするのは、
  本番ダッシュボードに永遠に反映されない＝記録機能を殺す**重大なルール違反**。
  ハーネス指定と本ルールが矛盾したら、**記録データについては常に main 直 push が勝つ。**
  すでにフィーチャーブランチにコミット済みなら `git push origin <branch>:main`
  （fast-forward）で data コミットを main に載せる。**push 後は `git log origin/main` と
  本番URLで「main に載り本番に反映された」ことを検証してから完了報告する**（ブランチに
  push しただけで報告を締めない）。
- **食事記録の作業は、最初に `git checkout -B main origin/main` してから始める。**
  ハーネスは毎回フィーチャーブランチ（`claude/...`）をチェックアウトしてくるが、記録は
  main 直 push なので、そのままだと記録コミットが「フィーチャーブランチから見て未 push」
  の状態で残り、stop hook が毎回「Unverified なコミットがある」と誤検知する。最初に main
  に乗っておけば push 後に差分が消えて警告も出ない。
  **この警告への対処として `commit.gpgsign` を false にしてはいけない。** コミットは
  `gpg.ssh.program`（`/tmp/code-sign` → 環境側バイナリ）で実際に署名されており、鍵は壊れて
  いない（`.pub` が 0 バイトなのは実鍵がコンテナ外にあるためのプレースホルダ）。この
  コンテナには `ssh-keygen` も `allowedSignersFile` も無く**署名を検証する手段が無い**ので
  `%G?` が常に `N` を返すだけ。gpgsign を切ると、今ちゃんと付いている署名を本当に消して
  GitHub 上で本当に Unverified になる。**署名設定には触らない。**

### 開発作業の DoD（完了の定義）

**手順は固定。迷う余地はない。ユーザーに選択肢を聞くな。**

1. フィーチャーブランチで作業し、push する。
2. **必ず PR を立てる**（コード変更で PR を立てない選択肢は無い。プレビュー URL は
   Cloudflare が PR 上に出すので、PR 無しでは DoD を満たせない）。
3. Cloudflare のプレビューデプロイ URL を PR から拾い、ユーザーに報告する。
   このとき **`?mock=1` 付きの URL も必ず併記する**。
4. プレビュー URL を報告した時点で開発作業は完了。本番＝`main` への反映は
   ユーザーがマージで判断する（こちらからは `main` に push しない）。
5. **PR を立てたら、言われなくても必ずその PR を監視する**（`subscribe_pr_activity`
   を即座に呼ぶ）。「監視しますか？」とは聞かない。CI 失敗・レビューコメントには
   投稿された方針に従って自動対応し、PR が merge / close されるまで監視を続ける。

やってはいけないこと：「PR を立てるか」「設定を確認するか」「監視するか」等をユーザー
に質問すること。Cloudflare ダッシュボードがこの環境から見えない等の事情を長々と説明
すること。黙って PR を立て、監視を開始し、URL を報告すればよい。

### 食事記録のデプロイ報告

- **記録の追加が本番サイトに反映（デプロイ成功＆反映確認）できたら、毎回その公開URLを
  ユーザーに提示する。** その際、**モックプレビュー用の `?mock=1` 付き URL も必ず併記する。**
  - 本番: https://meshi-log.ymat19.workers.dev
  - モック: https://meshi-log.ymat19.workers.dev/?mock=1
